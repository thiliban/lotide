/*
This function is used to accept an array and return the first element of the array
*/
const assertEqual = require('./assertEqual');
const head = function(lst) {
    return lst[0];
};

module.exports = head;
